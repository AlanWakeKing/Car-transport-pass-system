# 🚀 Инструкция по установке и запуску системы управления пропусками

## Шаг 1: Установка PostgreSQL

### Для Windows:
1. Скачай PostgreSQL с официального сайта: https://www.postgresql.org/download/windows/
2. Запусти установщик и следуй инструкциям
3. **Запомни пароль**, который укажешь для пользователя `postgres`
4. По умолчанию PostgreSQL будет на порту `5432`

### Для Linux (Ubuntu/Debian):
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

### Для macOS:
```bash
brew install postgresql
brew services start postgresql
```

## Шаг 2: Создание базы данных

1. Открой терминал (или CMD в Windows)
2. Подключись к PostgreSQL:

**Windows:**
```cmd
psql -U postgres
```

**Linux/macOS:**
```bash
sudo -u postgres psql
```

3. Создай новую базу данных:
```sql
CREATE DATABASE propusk_system;
```

4. Выйди из psql:
```sql
\q
```

## Шаг 3: Установка Python

Убедись, что у тебя установлен **Python 3.10 или выше**

Проверить версию:
```bash
python --version
```

Если Python не установлен:
- **Windows:** https://www.python.org/downloads/
- **Linux:** `sudo apt install python3.10 python3-pip`
- **macOS:** `brew install python@3.10`

## Шаг 4: Создание виртуального окружения

1. Создай папку для проекта:
```bash
mkdir propusk_system
cd propusk_system
```

2. Создай виртуальное окружение:
```bash
python -m venv venv
```

3. Активируй виртуальное окружение:

**Windows:**
```cmd
venv\Scripts\activate
```

**Linux/macOS:**
```bash
source venv/bin/activate
```

После активации в начале строки появится `(venv)`

## Шаг 5: Установка зависимостей

Сохрани файл `requirements.txt` в папку проекта и выполни:

```bash
pip install -r requirements.txt
```

Это установит все необходимые библиотеки.

## Шаг 6: Настройка подключения к БД

Открой файл `database.py` и измени строку подключения:

```python
DATABASE_URL = "postgresql://postgres:твой_пароль@localhost:5432/propusk_system"
```

Где:
- `postgres` - имя пользователя PostgreSQL
- `твой_пароль` - пароль, который ты указал при установке
- `localhost` - адрес сервера (если БД на твоём компьютере)
- `5432` - порт PostgreSQL (по умолчанию)
- `propusk_system` - имя базы данных, которую мы создали

## Шаг 7: Создание таблиц в БД

Убедись, что находишься в папке проекта и виртуальное окружение активно.

Запусти скрипт инициализации:

```bash
python init_db.py
```

Скрипт:
1. ✅ Проверит подключение к базе данных
2. ✅ Спросит, нужно ли пересоздать таблицы (при первом запуске - `yes`)
3. ✅ Создаст все необходимые таблицы
4. ✅ Покажет список созданных таблиц

### Ожидаемый результат:

```
============================================================
   ИНИЦИАЛИЗАЦИЯ БАЗЫ ДАННЫХ СИСТЕМЫ УПРАВЛЕНИЯ ПРОПУСКАМИ
============================================================

Шаг 1: Проверка подключения к базе данных
------------------------------------------------------------
✅ Подключение к базе данных успешно!

Шаг 2: Создание таблиц
------------------------------------------------------------

⚠️  Если таблицы уже существуют, они будут пересозданы (все данные будут потеряны!)
Пересоздать таблицы? (yes/no): yes

🔧 Начинаю создание таблиц...
✅ Все таблицы успешно созданы!

📋 Список таблиц в базе данных:
==================================================
  • users                     - Пользователи системы
  • organiz                   - Организации
  • abonent                   - Владельцы автомобилей
  • mark_auto                 - Марки автомобилей
  • model_auto                - Модели автомобилей
  • propusk                   - Пропуска (активные)
  • propusk_archive           - Архив отозванных пропусков
  • propusk_history           - История изменений пропусков
  • notifications_log         - Лог уведомлений
==================================================

✅ База данных готова к работе!

============================================================
   ИНИЦИАЛИЗАЦИЯ ЗАВЕРШЕНА
============================================================
```

## Проверка созданных таблиц

Можешь проверить таблицы через psql:

```bash
psql -U postgres -d propusk_system
```

Затем в psql:
```sql
\dt
```

Это покажет список всех таблиц.

---

## ❓ Возможные проблемы и решения

### Ошибка: "psql: FATAL: password authentication failed"
**Решение:** Проверь пароль в файле `database.py`

### Ошибка: "could not connect to server"
**Решение:** Убедись, что PostgreSQL запущен:
- Windows: Проверь в "Службы" (services.msc)
- Linux: `sudo systemctl status postgresql`

### Ошибка: "ModuleNotFoundError: No module named 'sqlalchemy'"
**Решение:** Убедись, что виртуальное окружение активно и выполни `pip install -r requirements.txt`

### Ошибка: "database 'propusk_system' does not exist"
**Решение:** Создай базу данных через psql (см. Шаг 2)

---

## 📞 Готово к тестированию!

Если всё прошло успешно и ты видишь ✅, значит база данных готова!

**Сообщи мне результат запуска скрипта `init_db.py`** - и мы продолжим дальше! 🚀